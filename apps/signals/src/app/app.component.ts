import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  imports: [],
  standalone: true,
  template: ``,
  styles: ``,
})
export class AppComponent {
  title = 'signals';
}
